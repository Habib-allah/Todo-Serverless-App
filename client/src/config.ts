
const apiId = 'bla'
export const apiEndpoint = `https://${apiId}.execute-api.us-east-1.amazonaws.com/dev`

export const authConfig = {
  
  domain: 'bla',            // Auth0 domain
  clientId: 'bla',          // Auth0 client id
  callbackUrl: 'http://localhost:3000/callback'
}
